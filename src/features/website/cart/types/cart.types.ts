export type CartItem = {
  slug: string;
  title: string;
  author: string;
  price: string;
  cover: string;
  quantity: number;
};

export type CartBenefit = {
  title: string;
  description: string;
};
