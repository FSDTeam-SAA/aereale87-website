import type { LucideIcon } from "lucide-react";

export type MissionItem = {
  title: string;
  description: string;
  icon: LucideIcon;
};

export type WhyWonderItem = MissionItem;

export type EcosystemItem = {
  title: string;
  description: string;
  icon: LucideIcon;
};
